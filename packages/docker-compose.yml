version: "3"

services:
  cache:
    image: redis:latest
    container_name: cache
    restart: always
    tty: true
    ports:
      - $REDIS_PORT:$REDIS_PORT
    command: >
      --requirepass ${REDIS_PASSWORD}
    networks:
      - application

networks:
  application:
    external: true
